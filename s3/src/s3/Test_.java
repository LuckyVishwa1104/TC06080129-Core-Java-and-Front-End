package s3;

public class Test_ extends A{
	public static void main(String[] args)
	{
		Pop_Corn p = new Pop_Corn()
		{
			
			@Override
			public void taste()
			{
				System.out.println("Chessy");
			}
		};
		p.taste();
	}
}

// create an abstract class Processor with int members variables data and method showdata to display data value 
// create abstract method process() to defined processing of memebers data.
//a. Create a class factorial using abstract class Processor to calculate and print factoriAL of a number 
//by overrideing the process method
/*b. create a class Circle using abstract class Processor to calculate and print are of a circle by overridding th process method
 *Ask user to enter choice (factorial or ciecle area). Also 
 *
 * 46). explain the importance of toString and equals method of the object class and override then on clas 
 * employee(empId, name, Salary).
 * a. create a class for main method (say XYZ) and except five employee information and store in an array.
 * Also ensure if neters empId is alresady exist or not(use equals method)
 * b. Display all employee info; using to stin g method*/
 